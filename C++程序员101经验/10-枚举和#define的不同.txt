1.为某些属性定义一组可选择的预定义的值。例如文件的状态，NPC的状态等等。
用#define方法，虽然能满足要求，但是它一个明显的缺点是：没有指出这些值之间的关联。
2.每个枚举成员都是一个常量，#define宏值不是常量
所以枚举值的初始化必须是一个常量表达式，初始化之后，其值不可被改变。枚举类型主要用于限制性输入。
但是宏可以改变，它是一种预处理替换符。
3.enum枚举具有类型，而#define宏没有，而且宏没有作用域。
4.枚举型变量的赋值必须用枚举值，不可以用int型数字。

