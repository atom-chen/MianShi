1.变量的位置
建议变量定义得离使用位置越近越好，尽量避免变量作用域膨胀。这种方式可以有效减少命名污染问题，同时可以提高代码的可读性和执行效率。
尽量延后变量的定义，直到非得使用变量前的那一时刻。
//string strSubStr;
//strSubStr=str.substr(iPos);

//string strSubStr(str.substr(iPos));
第一段代码是先用默认构造函数初始化，然后赋值
第二段代码是直接初始化一个有效值