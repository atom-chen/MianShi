1.静态类
使用静态类来包含不与特定对象关联的方法。例如，创建一组不操作实例数据并且不与代码中的特定对象关联的方法是很常见的要求。您应该使用静态类来包含那些方法。
（1）仅包含静态成员；
（2）无法实例化；
（3）静态类的本质，是一个抽象的密封类，所以不能被继承，也不能被实例化；
（4）不能包含实例构造函数；静态类不能包含构造函数，但仍可声明静态构造函数以分配初始值或设置某个静态状态。
（5）如果一个类下面的所有成员，都需要被共享，那么可以把这个类定义为静态类；