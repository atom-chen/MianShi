1.一个指针，无论是指向什么类型，其本身大小是固定的。
但是不同类型的指针有什么不同呢？
从内存大小的角度，没有什么不同，它们的不同主要是寻址得到的对象不同，因为指针类型会告诉编译器如何解释特定地址的内存内容和大小。
比如：一个指向地址1000的32位int指针，涵盖地址1000-1003，一个指向void的指针，不能够对其进行操作，因为不知道它的地址空间。
转型(cast)其实是一种编译器指令，dynamic_cast除外，大部分情况它并不改变一个指针所含的真正地址，它只影响“被指内存的大小和其内容”的解释方式。